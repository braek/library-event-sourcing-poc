spring:
  datasource:
    url: jdbc:postgresql://localhost:6000/sandbox
    username: sandbox
    password: sandbox
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none